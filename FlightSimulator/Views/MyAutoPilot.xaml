<UserControl x:Class="FlightSimulator.Views.MyAutoPilot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="txtAutoPilot" Grid.ColumnSpan="2" Height="400" VerticalAlignment="Top" BorderBrush="Blue"
        Background="{Binding Text, ElementName=txtAutoPilot}"/>

        <Button  Name="OKButton" Content="OK"  Margin="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                 FontWeight="DemiBold" Grid.Row="1" Grid.Column="0"  Height="20" Width="50"/>

        <Button  Name="ClearButton" Content="Clear"  Margin="8" HorizontalAlignment="Left" VerticalAlignment="Bottom" 
                 FontWeight="DemiBold" Grid.Row="1" Grid.Column="1" Height="20" Width="50"/>

    </Grid>
</UserControl>
